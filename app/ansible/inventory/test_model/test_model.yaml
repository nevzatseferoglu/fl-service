# code: language=ansible

all:
  children:
    flower_server:
      hosts: {}
    flower_clients:
      hosts:
        host_1:
          ansible_host: 192.168.1.105
          ansible_user: suav
          ansible_become_password: abc123
  vars:
    ansible_connection: ssh
