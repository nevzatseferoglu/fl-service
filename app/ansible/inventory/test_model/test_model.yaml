# code: language=ansible

all:
  children:
    flower_server:
      hosts:
        host_1:
          ansible_host: 192.168.1.105
          ansible_user: suav
    flower_clients:
      hosts: {}
  vars:
    ansible_connection: ssh
